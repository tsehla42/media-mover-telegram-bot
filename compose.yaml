# Docker Compose reference guide https://docs.docker.com/go/compose-spec-reference/

services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - "3001:3000"
    env_file:
      - .env
